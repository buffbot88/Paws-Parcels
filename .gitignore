node_modules/
dist/
server/dist/
coverage/
*.log
.DS_Store
/.freebuff

# Unified server config — contains real DB password and JWT secret.
# Commit server_config.example.json instead; copy it locally and fill in secrets.
server_config.json

# src/data: game-data JSON (content + maps) is committed; the large art packs
# (CloverVillage/HappyValley/VoidDesert/OtherAssets/Classes PNGs, EPS, AI) are
# imported locally and added manually when needed — never by plain `git add .`.
src/data/*
!src/data/*.json
!src/data/maps/
src/data/maps/*
!src/data/maps/*.json
