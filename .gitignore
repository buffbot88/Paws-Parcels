node_modules/
dist/
server/dist/
coverage/
*.log
.DS_Store
/.freebuff
oraclehost_id_rsa
.deploy_key

# Scratch / review artifacts (npm cache, ad-hoc notes)
tmp/

# Unified server config — contains real DB password and JWT secret.
# Commit server_config.example.json instead; copy it locally and fill in secrets.
server_config.json

# Keep the live SQLite database outside Git so rebuilds and pulls cannot replace
# player accounts or characters.
/persist/
# Keep this repository private if it contains real account/character data; never
# commit server_config.json, credentials, tokens, or production secrets.
# The full raw art archive (reference/assets/) is committed so the whole team has
# it. It contains the game's PNG art, Aseprite/Illustrator/EPS sources, and the
# Tiled map sources. Only macOS junk and the three >100 MB vector source files
# (tracked via Git LFS) are handled specially.
reference/**/.DS_Store

src/data/*
!src/data/*.json
!src/data/maps/
src/data/maps/*
!src/data/maps/*.json
Alpha.md
