# Generated BrowseAssets catalog

Do not edit this directory manually. Rebuild it with:

```bash
npm run assets:browse
```

The output is derived from `BrowseAssets.html`, `scripts/build-browse-assets.mjs`, and `design/assets/asset-inventory.json`. It references the deployed `reference/assets/` archive.
