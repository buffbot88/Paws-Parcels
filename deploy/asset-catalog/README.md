# Generated asset catalog

Do not edit this directory manually. Rebuild it from the repository sources with:

```bash
npm run assets:catalog
```

The output is derived from `design/assets/asset-inventory.json`, `design/assets/asset-reviews.json`, `reference/assets/`, and the templates/generators in `Asset Catalog/`. It is tracked only so static deployment can consume a checkout directly.
